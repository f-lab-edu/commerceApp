package com.example.commerceapp.domain.model

enum class ErrorType {
    NO_CONTENT,
    INVALID,
    UNAUTHORIZED,
    FORBIDDEN,
    NOT_FOUND,
    CONFLICT,
    UNKNOWN_ERROR,
    SERVER_ERROR,
    NETWORK_ERROR
}